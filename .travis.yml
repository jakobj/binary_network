language: python
python:
  - "3.3"
  - "2.7"
  - "2.6"
branches:
  only:
    - master
install:
  - pip install numpy
  - pip install python-coveralls
  - pip install nose
  - pip install coverage
  - export PYTHONPATH=$PYTHONPATH:.
script:
  - nosetests --with-coverage
after_success:
  - coveralls